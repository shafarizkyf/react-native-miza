const prefixes = ['https://miza.shafarizkyf.com', 'miza://'];

const config = {
  screens: {
    ForgotPasswordScreen: 'forgot-password',
    SignupScreen: 'register',
  },
};

export const linking = {
  prefixes,
  config,
};
